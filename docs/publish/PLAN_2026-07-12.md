# Publish plan for autoaim_research

Date: 2026-07-12

## Goal

Publish this workspace to the public GitHub repository `Chr0mium24/autoaim_research`.

## Plan

1. Keep the existing `docs/arch/` research archive, including copied source, config, model, and asset files.
2. Register the complete third-party repositories under `open_source_vision_repos/` as Git submodules instead of adding their full working trees to the outer repository.
3. Add a root `README.md` that explains the archive layout, clone commands, and third-party code notice.
4. Create or configure the GitHub repository as public, add it as `origin`, and push the cleaned workspace.
5. Record the publication result in a markdown results document.

## Checks

- Confirm no tracked file exceeds GitHub's 100 MB single-file limit.
- Confirm all submodules are pinned to the intended commits.
- Confirm the outer working tree is clean after commit and push.
